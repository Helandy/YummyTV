package su.afk.yummy.tv.core.designsystem.locals

import androidx.compose.runtime.staticCompositionLocalOf

/** Whether the device currently has no validated internet connection. */
val LocalIsOffline = staticCompositionLocalOf { false }
