package su.afk.yummy.tv.feature.player.mobile.model

/** Действие плеера по клавише физической клавиатуры (планшет, Chromebook, оконный режим). */
internal enum class MobilePlayerKeyAction {
    PlayPause,
    SeekBackward,
    SeekForward,
}
